services:
  app:
    build: .
    restart: unless-stopped
    environment:
      DATABASE_URL: file:/data/turni.db
      APP_URL: ${APP_URL}
      APP_ENCRYPTION_KEY: ${APP_ENCRYPTION_KEY}
      SESSION_SECRET: ${SESSION_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      AI_PROVIDER: ${AI_PROVIDER:-groq}
      AI_FALLBACK_PROVIDER: ${AI_FALLBACK_PROVIDER:-}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      GROQ_MODEL: ${GROQ_MODEL:-qwen/qwen3.8-27b}
      GROQ_MAX_OUTPUT_TOKENS: ${GROQ_MAX_OUTPUT_TOKENS:-4000}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      ANTHROPIC_MODEL: ${ANTHROPIC_MODEL:-claude-opus-5}
      IMAGE_RETENTION_DAYS: ${IMAGE_RETENTION_DAYS:-90}
      # Senza questa, WORKDIR /app fa risolvere il default dentro il layer
      # dell immagine invece che nel volume montato su /data: le foto (dati
      # personali di terzi) sparirebbero a ogni ricostruzione del container.
      UPLOAD_DIR: /data/uploads
      TZ: Europe/Rome
    volumes:
      - turni-data:/data
    healthcheck:
      test: ['CMD', 'wget', '--spider', '-q', 'http://localhost:3000/api/health']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    # Il token come variabile d ambiente non finisce nell argomento del comando
    # (visibile con "docker inspect"), a differenza di --token.
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    command: tunnel --no-autoupdate run
    depends_on:
      app:
        condition: service_healthy

volumes:
  turni-data:
